pub mod canister;

// use canister::PaymentCanister;

fn main() {
    // let canister_e_idl = PaymentCanister::idl();
    // let idl =
    //     candid::bindings::candid::compile(&canister_e_idl.env.env, &Some(canister_e_idl.actor));

    // println!("{}", idl);
}
